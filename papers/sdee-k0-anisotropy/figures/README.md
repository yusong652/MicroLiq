# Figures

Place manuscript figures used by `paper/main.tex` here.

Recommendations:

- Use vector `PDF` for line plots whenever possible.
- Use high-resolution `PNG` for raster images / rendered snapshots.
- Keep filenames stable and descriptive (e.g., `macro_response_4panel.pdf`).
- Avoid thesis-only numbering in filenames; prefer semantic names.

